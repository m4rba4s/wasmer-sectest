[package]
name = "wasmer-demo"
version = "0.1.0"
edition = "2024"

[dependencies]
wasmer = { version = "=6.1.0", features = ["sys", "cranelift", "singlepass"] }
wat = { version = "=1.249.0" }

[lib]
name = "wasmer_demo"
path = "src/lib.rs"
